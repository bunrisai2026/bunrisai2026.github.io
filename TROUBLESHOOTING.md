# これまで起きた不具合と対策の記録

このサイトを作る過程で実際に起きた不具合と、その原因・対策をまとめています。
同じような問題が今後起きたときに、まずここを確認してください。

## 1. 画像を差し替えたのに古い画像のまま表示され続けた

**原因**：`images/hero-stage.jpg` のように同じファイル名のまま中身だけ差し替えると、
ブラウザ（や配信元のCDN）が「このURLの画像は前に見たから再取得しなくていい」と
判断し、キャッシュされた古い画像を表示し続けることがある。

**対策**：
- 画像を差し替えるときは、できるだけ**新しいファイル名**にする（例: `hero-courtyard.jpg`）
- どうしても同じ名前を使うときは、参照側に `?v=2` のようなバージョン番号を付け、
  差し替えるたびに数字を増やす

## 2. スマホでタブ（メニュー）が画面から見切れる／指で触ると斜めに動く

**原因**：CSSのグリッド/フレックスボックスは、中身の分だけ子要素が横に広がろうとする
性質があり、意図せずページ全体が横方向にもスクロールできる状態になっていた。

**対策**（`css/style.css` に反映済み）：
- `html, body { overflow-x: hidden; }` でページ全体の横スクロールを禁止
- `body { touch-action: pan-y; }` で指の操作を縦方向だけに制限
- グリッド/フレックスの子要素に `min-width: 0` を指定し、無理に広がらないようにする

## 3. スクロールで現れる演出が原因で、セクションが消えたまま戻らなくなった

**原因**：スクロールで要素がふわっと現れる演出を「最初は透明(opacity:0)にしておき、
JSが動いたら表示する」という作りにしていたため、JSが何か1つでも失敗すると、
それ以降の処理が止まり、要素が透明のまま永久に戻らなくなっていた。

**対策**（`js/main.js`, `css/style.css` に反映済み）：
- 要素は**デフォルトで表示**にしておき、JSが正常に動いたときだけ`.js-reveal`という
  目印を付けて、そのときだけ透明→表示のアニメーションをする方式に変更
- 演出のON/OFF・スクロール処理・カウントダウンなど、それぞれの初期化処理を
  `try/catch`で独立させ、1つが失敗しても他が止まらないようにした

## 4. Artifactのプレビューと本番サイトを混同していた

**原因**：Claudeの会話内で見られる「Artifactのプレビュー」（claude.ai上の隔離された
コピー）と、実際に公開されている本番サイト（GitHub Pages）は完全に別物。
プレビュー側を更新しても本番には反映されず、逆に本番を直してもプレビュー側は
古いままなので、「直したはずなのに変わっていない」という混乱が何度か起きた。

**対策**：以後は本番サイトの変更を都度Artifact側にも反映するようにした。
基本的に確認は本番URL（https://bunrisai2026.github.io/）で行うのが確実。

## 5. ヒーロー背景の写真がCSSの`background-image`だと全く描画されない

**これが一番原因の特定に苦労した不具合です。**

**症状**：サーバーからは正しい画像が届いており（HTTPステータス・ファイルサイズとも
正常）、CSSの記述にも誤りがないのに、実際にブラウザで開くと写真部分が
グラデーションだけの状態になり、写真が一切表示されなかった。

**調査した内容**：
- 画像ファイル自体の破損 → PILで検証したが問題なし
- CSSの構文ミス → 何度確認しても正しい
- `position: sticky` が原因か → `position: relative`に変えても再現
- 上に重なるcanvas（粒子演出）が原因か → canvasを非表示にしても再現
- `css/style.css` 自体のキャッシュ → `<link>`タグに`?v=`が付いておらず、
  画像だけキャッシュ対策してもCSSファイル自体が更新されていなかった疑いが強い
  （これが少なくとも一因だったと考えられる）
- ローカルで同一構造・同一画像を使った再現テストを行ったが、**そちらでは
  問題が再現しなかった**。そのため、GitHub Pages経由で配信したときに限って
  何らかの理由でSafariの描画に影響していた可能性が高いが、完全な原因の
  特定には至らなかった。

**最終的な対策（ワークアラウンド）**：
CSSの`background-image`で写真を指定するのをやめ、確実に表示される`<img>`タグで
写真を表示し、暗いグラデーションは別レイヤー(`.hero-overlay`という`div`)として
上に重ねる構成に変更した。実際にスクリーンショットで表示を直接確認済み。

**教訓**：装飾ではなく実質的なコンテンツとして写真を見せたいときは、
CSSの背景画像より`<img>`タグの方が確実（アクセシビリティ的にも本来はこちらが適切）。

## 6. CSS/JSファイル自体にキャッシュ対策をしていなかった

**原因**：画像には`?v=`を付けてキャッシュ対策をしていたが、それを読み込む側の
`css/style.css`や`js/main.js`自体の読み込みタグにはバージョン番号が付いておらず、
ブラウザがCSS/JS自体を更新後もしばらく古いまま使い続けていた可能性がある。

**対策**：`index.html`内の
```html
<link rel="stylesheet" href="css/style.css?v=4">
<script src="js/main.js?v=4"></script>
```
のように、CSS/JSファイル自体にもバージョン番号を付けた。**デザインやJSの動きを
大きく変えたときは、この`v=`の数字を1つ増やしてから公開する**ようにすると、
今回のような「直したのに反映されない」という混乱を避けやすい。

## 7. サーバー側の確認だけでは見た目の不具合に気づけなかった

**原因**：`curl`でHTTPステータスコードやファイルサイズを確認しても「200 OK・
サイズも正しい」という結果しか分からず、実際にブラウザでどう見えているかは
別問題だった。今回の写真が表示されない不具合は、まさにこのパターンだった。

**対策**：見た目に関わる修正をしたときは、実際にスクリーンショットを撮って
目視で確認する。ステータスコードが正常なことは「直った証拠」にはならない。
